#!/bin/sh

if [ $# != 1 ] && [ $# != 2 ]; then
	echo 'usage: clandro-open-url <url> [app_package_or_component]'
	echo 'Open an URL for viewing.'
	exit 1
fi

case "${CLANDRO__USER_ID:-}" in ''|*[!0-9]*|0[0-9]*) CLANDRO__USER_ID=0;; esac

am start --user "$CLANDRO__USER_ID" -a android.intent.action.VIEW -d "$@" > /dev/null
